name: Cache Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Cache Test
        uses: actions/cache@v3
        with:
          path: ./test-cache
          key: ${{ runner.os }}-test-${{ github.ref_name }}
